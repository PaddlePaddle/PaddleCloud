apiVersion: batch.paddlepaddle.org/v1
kind: PaddleJob
metadata:
  name: paddlejob-2
spec:
  worker:
    replicas: 2
    template:
      spec:
        containers:
          - name: sample
            image: nginx
        restartPolicy: OnFailure
  ps:
    replicas: 1
    template:
      spec:
        containers:
          - name: sample
            image: nginx
        restartPolicy: OnFailure
    
