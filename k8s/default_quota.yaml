apiVersion: v1
kind: ResourceQuota
metadata:
  name: compute-resources
spec:
  hard:
    pods: "10"
    requests.cpu: "10"
    requests.memory: 100Gi 
    limits.cpu: "10"
    limits.memory: 100Gi
    persistentvolumeclaims: "1"
